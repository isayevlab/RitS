# Examples

Each subfolder contains runnable examples for one reaction family.

Contents:
- `cmd.sh`: command(s) to run sampling for that example
- `reaction.svg` (or `reaction_E.png` / `reaction_Z.png`): reaction scheme image(s)
